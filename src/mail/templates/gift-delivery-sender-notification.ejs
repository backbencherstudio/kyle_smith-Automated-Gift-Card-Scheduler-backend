<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Gift Delivered Successfully</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #4CAF50; color: white; padding: 20px; text-align: center; border-radius: 5px 5px 0 0; }
        .content { background: #f9f9f9; padding: 20px; border-radius: 0 0 5px 5px; }
        .gift-details { background: white; padding: 15px; margin: 15px 0; border-radius: 5px; border-left: 4px solid #4CAF50; }
        .footer { text-align: center; margin-top: 20px; color: #666; font-size: 12px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🎁 Gift Delivered Successfully!</h1>
        </div>
        
        <div class="content">
            <p>Hello <strong><%= sender_name %></strong>,</p>
            
            <p>Great news! Your gift has been successfully delivered to <strong><%= recipient_name %></strong>.</p>
            
            <div class="gift-details">
                <h3>Gift Details:</h3>
                <ul>
                    <li><strong>Recipient:</strong> <%= recipient_name %> (<%= recipient_email %>)</li>
                    <li><strong>Vendor:</strong> <%= vendor_name %></li>
                    <li><strong>Value:</strong> $<%= face_value %></li>
                    <li><strong>Delivery Date:</strong> <%= new Date(scheduled_date).toLocaleDateString() %></li>
                    <% if (custom_message) { %>
                        <li><strong>Your Message:</strong> "<%= custom_message %>"</li>
                    <% } %>
                </ul>
            </div>
            
            <p>Your gift card has been sent to <strong><%= recipient_email %></strong> and should be available for use immediately.</p>
            
            <p>Thank you for using our gift scheduling service!</p>
        </div>
        
        <div class="footer">
            <p>This is an automated notification from your gift scheduling service.</p>
        </div>
    </div>
</body>
</html> 